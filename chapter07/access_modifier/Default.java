package chapter07.accessmodifier; // 패키지가 동일하다

public class Default {

    // default (접근 제어자가 없는 변수, 메소드는 default 접근 제어자가 되어 해당 패키지 내에서만 접근 가능)
        String lastname = "kim"; // lastname은 default 접근 제어자로 설정된다.
}
